{
  "name": "fueltracks-ims",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "install-all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "node backend/src/index.js",
    "backend": "npm --prefix backend start",
    "frontend": "npm --prefix frontend run dev",
    "dev": "npx concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "seed": "npm --prefix backend run seed",
    "clear": "npm --prefix backend run clear",
    "test": "npm --prefix backend test",
    "tunnel": ".\\tools\\cloudflared.exe tunnel --url http://localhost:5000",
    "online": ".\\tools\\ngrok.exe http --url=plant-disposal-accompany.ngrok-free.dev 5000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
